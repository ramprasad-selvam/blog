export const metadata = {
  title: "Contact Me | Ramprasad Portfolio",
  description: "Get in touch with Ramprasad for projects, collaborations, or queries via email or phone.",
};

import ContactForm from "./clientPage";

export default function ContactPage() {
  return <ContactForm />;
}
